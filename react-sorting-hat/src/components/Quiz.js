import React from "react";
import styled from "styled-components";

const Quiz = (props) => {
  const {} = props;
  return (
    <StyledQuiz>
      <h1>QUIZ</h1>
    </StyledQuiz>
  );
};

const StyledQuiz = styled.section``;

export default Quiz;
